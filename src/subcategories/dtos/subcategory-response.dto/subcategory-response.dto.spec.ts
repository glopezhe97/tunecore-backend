import { SubcategoryResponseDto } from './subcategory-response.dto';

describe('SubcategoryResponseDto', () => {
  it('should be defined', () => {
    expect(new SubcategoryResponseDto()).toBeDefined();
  });
});
